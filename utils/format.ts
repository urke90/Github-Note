// /**
//  *
//  * @param dateAsString 'date got from the MongoDB as string
//  * @returns {string} 'Date returned is in format YYYY-MM-DD
//  */

// export const formatDateForHeatMap = (dateAsString: string) => {
//   const date = new Date(dateAsString);

//   return new Intl.DateTimeFormat('en-CA').format(date);
// };
